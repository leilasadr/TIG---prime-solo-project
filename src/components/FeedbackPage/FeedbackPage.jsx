import React from "react";
import { useSelector } from "react-redux";

function FeedbackPage(){
    return (
        <p>This is where the user will submit a color/text feedback.</p>
    )
};

export default FeedbackPage;